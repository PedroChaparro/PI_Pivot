<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>LOGIN</title>
        <link rel="stylesheet" href="/styles/main.css" />
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;500;700;900&display=swap"
            rel="stylesheet"
        />
    </head>
    <body>
        <div class="login-root">

            <div class="login">

                <%- include('_messages.ejs') %>

                <h1 class="login__title">LOGIN</h1>

                <form action="/login" method="post" id="login-form">

                    <div class="form-group" id="user_email-group">
                        <label class="label--block" for="user_email">Email</label>
                        <div class="form-group__input">
                            <input class="input--fw" type="email" id="user_email" name="user_email" placeholder="example@domain.com" />
                        </div>
                        <p class="form-group__error-message">El correo electrónico no es válido.</p>
                    </div>

                    <div class="form-group" id="user_password-group">
                        <label class="label--block" for="user_password">Password</label>
                        <div class="form-group__input">
                            <input class="input--fw" type="password" id="user_password" name="user_password" placeholder="********" />
                        </div>
                        <p class="form-group__error-message">La contraseña debe contener mínimo 8 caracteres y debe incluír un número y uno de los siguientes caracteres especiales: @ $ ! % * # ? & / %</p>
                    </div>

                    <p class="form__error-message">Por favor, rellene el formulario correctamente antes de realizar el envío.</p>

                    <input type="submit" value="ENVIAR" class="btn btn--primary btn--block btn--form" />

                    <p class="login__redirect">Doesn´t have an account?, <a href="/signup">create one</a></p>
                </form>
            </div>
        </div>

        <script src="/scripts/login_validate.js"></script>
        <script src="/scripts/alerts.js"></script>
        <script src="https://kit.fontawesome.com/5023e2a492.js" crossorigin="anonymous"></script>

    </body>
</html>
